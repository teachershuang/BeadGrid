你是一名高级 TypeScript、React、Tauri 桌面应用工程师和图像算法工程师。

请从零开发一个开源的 Windows 拼豆底稿生成工具，暂定项目名为 BeadPattern。

项目面向普通拼豆用户，不面向开发者。核心目标是让用户导入一张图片，快速生成颜色准确、杂色较少、方便照图拼制的拼豆底稿，并自动生成采购清单和分色图纸。

## 一、技术栈

* Tauri 2
* React
* TypeScript
* Vite
* Canvas 2D
* Web Worker
* Vitest
* GitHub Actions

应用完全离线运行，不需要服务器、账号系统或数据库。

首期只支持 Windows，通过 GitHub Release 发布安装版 EXE 和便携版压缩包。

不得复制其他拼豆项目的代码或色板数据。

## 二、MVP 用户流程

1. 用户导入 PNG、JPG、JPEG 或 WebP 图片。
2. 设置作品尺寸，例如 50×50、100×100 或自定义宽高。
3. 调整图片裁剪区域。
4. 选择拼豆品牌。
5. 设置最大颜色数量。
6. 设置杂色清理强度。
7. 生成拼豆图纸。
8. 查看每种色号的数量。
9. 导出完整底稿、分色底稿和采购清单。

## 三、作品尺寸与底板尺寸

必须区分：

* 作品尺寸：最终图纸的拼豆格数，例如 100×100。
* 实体底板尺寸：例如 29×29，只用于显示分界和拆分图纸。

实体底板尺寸不得影响颜色计算。

支持常用作品尺寸预设和自定义尺寸，MVP 最大支持 300×300。

## 四、图片处理

支持：

* 图片拖动和缩放
* 自动居中裁剪
* 保持比例填满
* 保持比例完整显示
* 90 度旋转
* 水平翻转
* 透明区域作为空豆
* 透明区域合成到指定背景色

不得简单将原图缩小到目标尺寸后直接读取像素，因为插值会产生灰边和脏色。

应为每个拼豆格计算其在原图中对应的像素区域，并直接分析该区域。

## 五、主导色算法

每个拼豆格使用主导色算法取色，而不是 RGB 平均值。

推荐流程：

1. 读取该格对应区域的有效像素。
2. 忽略透明度低于阈值的像素。
3. 对半透明像素进行正确的背景合成。
4. 将区域像素进行预量化，例如 RGB 每通道保留 5 位。
5. 统计每个量化颜色簇的像素数量。
6. 找到像素数量最多的颜色簇。
7. 从最大颜色簇中选择有代表性的真实像素作为该格目标色。

算法结果必须确定，相同输入和参数必须得到相同输出。

## 六、内置品牌色板

品牌色板由项目内置数据提供，不需要用户上传 CSV。

色板数据放在：

本层文件夹的csv 请你查看或者上网进一步查看

每个品牌使用独立 JSON 或 CSV 文件。

最低字段：

* 品牌
* 系列
* 色号
* 中文名称
* RGB

程序启动时读取并校验色板数据。

内部每个颜色至少包含：

```ts
interface PaletteColor {
  id: string;
  brandId: string;
  seriesId: string;
  code: string;
  nameZh?: string;
  rgb: {
    r: number;
    g: number;
    b: number;
  };
  lab: {
    l: number;
    a: number;
    b: number;
  };
}
```

品牌切换时，必须从每个格子的原始目标颜色重新映射，不能从旧品牌色号转换到新品牌色号。

## 七、颜色匹配

不得使用简单 RGB 欧氏距离作为最终匹配算法。

必须实现：

* sRGB 转 Linear RGB
* Linear RGB 转 XYZ
* XYZ 转 CIELAB
* CIEDE2000 色差

将每个格子的目标颜色匹配到当前品牌色板中感知色差最小的色号。

CIEDE2000 必须编写单元测试。

## 八、最大颜色数量

用户可以设置最大颜色数量：

* 0：不限制颜色数量，尽量还原原图。
* X：最终实际使用色号数量不得超过 X。

空豆不计入颜色数量。

当设置颜色上限时，需要直接从当前品牌色板中选出最多 X 种颜色，使整张图的加权色差尽可能小。

禁止只做 K-Means 后再简单映射品牌色号。

推荐使用确定性的贪心选色：

1. 汇总所有格子的目标 Lab 和出现次数。
2. 选择最能降低整图总色差的品牌颜色。
3. 不断增加颜色，直到达到上限。
4. 将所有格子映射到已选颜色中最接近的颜色。
5. 可增加简单的局部交换优化。

必须保证：

```text
actualColorCount <= maxColors
```

## 九、自动清理杂色

使用 BFS、DFS 或等价连通区域算法识别孤立的小色块。

处理流程：

1. 找出由相同色号组成的连续区域。
2. 找出面积小于阈值的区域。
3. 收集与该区域接触的邻近颜色。
4. 根据颜色差异和公共边界长度选择最佳合并目标。
5. 不允许合并到色差过大的颜色。
6. 重复处理，直到稳定或达到最大轮数。

用户界面只提供：

* 关闭
* 轻度
* 中度
* 强力

高级参数可以保留在代码中，但 MVP 不需要向普通用户显示。

## 十、结果预览

使用 Canvas 2D 显示拼豆图纸，不要为每个格子创建 React DOM 元素。

支持：

* 缩放
* 平移
* 适应窗口
* 显示或隐藏网格
* 显示或隐藏色号
* 显示或隐藏底板分界
* 原图与生成结果切换
* 点击某个颜色，只高亮该颜色
* 鼠标悬停显示坐标、色号和颜色名称

MVP 暂时不实现完整手动编辑器。

可以保留两个简单操作：

* 将一种色号全局替换成另一种色号
* 将单个格子设为空豆

如果实现成本较高，这两个功能也可以延后。

## 十一、颜色统计和采购清单

自动统计：

* 作品尺寸
* 非空拼豆总数
* 空豆数量
* 实际颜色数量
* 品牌
* 系列
* 每种色号的实际数量

每种颜色显示：

* 色块
* 色号
* 中文名称
* 实际数量

支持设置采购预留比例：

* 0%
* 5%
* 10%

建议准备数量：

```text
ceil(实际数量 × (1 + 预留比例))
```

必须保证所有颜色数量总和等于非空格总数。

## 十二、导出功能

MVP 支持以下导出：

### 1. 完整底稿 PNG

可选择：

* 显示网格
* 显示色号
* 显示底板分界
* 显示坐标
* 显示颜色图例

### 2. 分色图纸

按每一个实际使用色号生成一张图。

当前颜色正常显示，其他颜色可以：

* 显示为浅灰
* 低透明度显示
* 完全隐藏

每张图显示：

* 品牌
* 色号
* 颜色名称
* 当前颜色总数量

所有分色图打包为 ZIP。

### 3. 采购清单

导出：

* PNG 图片
* CSV 文件

### 4. 底板拆分图

根据实体底板尺寸拆分图纸。

文件名示例：

```text
board_R01_C01.png
board_R01_C02.png
```

每张图显示分块编号和全局坐标。

## 十三、界面布局

使用简单的三栏布局。

左侧：

* 导入图片
* 作品尺寸
* 裁剪方式
* 品牌选择
* 最大颜色数量
* 杂色清理强度
* 生成按钮

中间：

* 原图和拼豆图纸预览
* 缩放和平移
* 网格和底板分界

右侧：

* 当前颜色列表
* 每种颜色数量
* 总拼豆数量
* 实际颜色数
* 导出按钮

处理图片时显示当前阶段和进度。

## 十四、Web Worker

以下操作必须放到 Web Worker 中：

* 单格像素采样
* 主导色提取
* Lab 转换
* 品牌颜色匹配
* 最大颜色数量计算
* BFS 杂色清理

Worker 支持：

* 处理进度
* 当前阶段
* 取消任务
* 错误返回
* 任务 ID

用户重新生成时，应取消旧任务，防止旧结果覆盖新结果。

## 十五、测试要求

至少为以下功能编写单元测试：

* RGB 转 Lab
* CIEDE2000
* 主导色提取
* 透明像素处理
* 最近品牌颜色匹配
* 最大颜色数量限制
* BFS 连通区域
* 小区域合并
* 颜色用量统计
* 底板拆分计算

必须测试：

1. 最大颜色数设置为 1 时，结果只有一种颜色。
2. 最大颜色数设置为 5 时，结果不超过 5 种颜色。
3. 透明区域不会生成黑边。
4. 采购清单总数量等于非空格数量。
5. 分色图的高亮格数量等于对应色号数量。
6. 相同输入和设置重复运行，结果一致。

## 十六、项目结构

建议结构：

```text
src/
  components/
  pages/
  stores/

  core/
    color/
    image/
    palette/
    quantization/
    cleanup/
    statistics/
    export/

  workers/
  types/
  constants/

resources/
  palettes/

tests/
  unit/
  fixtures/

src-tauri/
docs/
```

不要将算法代码写在 React 组件中。

不要创建单个超大组件。

## 十七、GitHub Release

创建 GitHub Actions：

当推送 `v*` Tag 时：

1. 安装依赖。
2. 执行 lint。
3. 执行 TypeScript 类型检查。
4. 执行单元测试。
5. 构建 Windows x64 应用。
6. 生成 NSIS 安装版 EXE。
7. 生成便携版 ZIP。
8. 创建 GitHub Release。
9. 上传构建产物。

发布文件示例：

```text
BeadPattern-Setup-x64.exe
BeadPattern-Portable-x64.zip
SHA256SUMS.txt
```

## 十八、MVP 暂不实现

以下功能暂时不要开发：

* 用户上传或编辑色板 CSV
* 完整手动画笔编辑器
* 撤销和重做系统
* 区域选择
* 填充工具
* 吸管工具
* 云端同步
* 用户账号
* 在线素材库
* 社区功能
* 自动更新
* 手机端
* macOS 和 Linux
* AI 生图
* 视频和 GIF
* 多图层
* 复杂项目文件格式
* PDF 导出
* 打印系统

代码结构可以为后续功能预留扩展点，但不要提前实现。

## 十九、开发顺序

按以下里程碑开发，每完成一个阶段都运行测试和构建：

1. 初始化 Tauri、React、Vite 和 TypeScript。
2. 实现内置色板读取和校验。
3. 实现 RGB、Lab 和 CIEDE2000。
4. 实现图片导入、裁剪和透明像素处理。
5. 实现单格区域采样和主导色算法。
6. 实现品牌颜色匹配。
7. 实现最大颜色数量限制。
8. 实现 BFS 杂色清理。
9. 将处理管线迁移到 Web Worker。
10. 实现 Canvas 图纸预览。
11. 实现颜色统计和采购清单。
12. 实现完整底稿导出。
13. 实现分色图和 ZIP 导出。
14. 实现底板拆分导出。
15. 配置 GitHub Actions 和 Windows Release。

开始开发前，先输出：

1. MVP 功能清单。
2. 项目目录设计。
3. 核心数据结构。
4. 图像处理管线。
5. 里程碑计划。

确认架构后再开始逐阶段编写代码，不要一次性生成整个项目。
