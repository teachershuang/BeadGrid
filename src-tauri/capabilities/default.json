{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "BeadGrid 主窗口默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:write-files"
  ]
}
